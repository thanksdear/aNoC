
// ============================================================
//  Include 搜索路径
// ============================================================
+incdir+../          // tb/  —— pkg.sv 里的 `include "env/..." 从这里找

// ============================================================
//  源文件（顺序不能乱）
// ============================================================

// 1. DUT
../../rtl/axi-lite-slave.sv

// 2. 接口（必须在 pkg 之前，interface 不能进 package）
../axi_lite_slave_if.sv

// 3. 验证环境包（内部按依赖顺序 `include 所有 env/ 类，含 test）
../axi_lite_slave_pkg.sv

// 4. 顶层
../top/tb.top.sv
